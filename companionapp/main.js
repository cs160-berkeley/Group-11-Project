import Pins from "pins";import {remotePins, pressed_inv_skin} from "variables";import {InventoryScreen, addItemButton} from "invScreen";import {headerAndNavBar} from "navbar";//************************************//@program/* Pins stuff */let mainScreen = new Container({	name: 'main',	left: 0, right: 0, top: 0, bottom: 0,	contents: [		InventoryScreen(),        addItemButton,		headerAndNavBar,	],});application.behavior = Behavior({    onLaunch(application) {        application.add(mainScreen);        application.main.hAndN.nav.invBtn.skin = pressed_inv_skin;        this.data = { labels: {} };        let discoveryInstance = Pins.discover(            connectionDesc => {                if (connectionDesc.name == "food-sensors") {                    trace("Connecting to remote pins\n");                    remotePins = Pins.connect(connectionDesc);                }            },            connectionDesc => {                if (connectionDesc.name == "food-sensors") {                    trace("Disconnected from remote pins\n");                    remotePins = undefined;                }            }        );	}});